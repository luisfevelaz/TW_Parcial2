import { PipeEnlacesPipe } from './pipe-enlaces.pipe';

describe('PipeEnlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEnlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
